# Rename this file to credentials.yaml and fill in your login details.
# One entry per profile (p1, p2, ...). The GUI reads/writes this format.
profiles:
  p1:
    email: #FILLYOUREMAIL
    password: #FILLYOURPASSWORD
    discord_webhook: ""  # Optional: per-profile Discord webhook for error/captcha alerts
  p2:
    email:
    password:
    discord_webhook: ""

# Legacy single-account format is still supported:
# mobage:
#   email: your-email@example.com
#   password: your-password
