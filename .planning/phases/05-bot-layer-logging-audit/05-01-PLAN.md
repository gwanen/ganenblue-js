---
wave: 1
depends_on: []
files_modified:
  - "src/bot/battle-handler.js"
  - "src/bot/quest-bot.js"
  - "src/bot/raid-bot.js"
  - "src/bot/skip-bot.js"
autonomous: true
---

# Phase 5 Plan: Bot Layer Logging Audit

## Goal
Audit and standardize logging strings in the bot logic layer to enforce a clean, consistent UI experience.

## Requirements Covered
- LOG-05

<task>
<action>
1. In `src/bot/battle-handler.js`:
   - Replace all instances of `[Wait]` with `[Battle]`.
   - Remove trailing periods from action logs.
   - Standardize `[Battle] Clicked ...` messages.
</action>
<read_first>
- src/bot/battle-handler.js
- .planning/phases/05-bot-layer-logging-audit/05-CONTEXT.md
</read_first>
<acceptance_criteria>
- `grep "\[Wait\]" src/bot/battle-handler.js` returns no results.
</acceptance_criteria>
</task>

<task>
<action>
1. In `src/bot/quest-bot.js`:
   - Replace `[Status]` with `[Quest]`.
   - Remove `✓` from log messages.
   - Standardize run progress strings to `run N/M`.
</action>
<read_first>
- src/bot/quest-bot.js
</read_first>
<acceptance_criteria>
- `grep "\[Status\]" src/bot/quest-bot.js` returns no results.
</acceptance_criteria>
</task>

<task>
<action>
1. In `src/bot/raid-bot.js`:
   - Replace `[Status]` with `[Raid]`.
   - Standardize error and retry messages.
</action>
<read_first>
- src/bot/raid-bot.js
</read_first>
<acceptance_criteria>
- `grep "\[Status\]" src/bot/raid-bot.js` returns no results.
</acceptance_criteria>
</task>

<task>
<action>
1. In `src/bot/skip-bot.js`:
   - Replace `[Status]` with `[Skip]`.
   - Polishing start/stop wording.
</action>
<read_first>
- src/bot/skip-bot.js
</read_first>
<acceptance_criteria>
- `grep "\[Status\]" src/bot/skip-bot.js` returns no results.
</acceptance_criteria>
</task>
