---
wave: 1
depends_on: []
files_modified:
  - "src/gui/index.html"
  - "src/gui/renderer.js"
  - "src/gui/main.js"
autonomous: true
---

# Phase 3: GUI Logging Standardization

## Goal
Standardize the GUI logging panel by adding `.log-level-success` styles, ensuring P1/P2 profile colors, and replacing redundant tags.

## Requirements Covered
- LOG-01
- LOG-02

<task>
<action>
1. In `src/gui/index.html`:
   - Find `.log-tag-status` (currently emerald) and rename it to `.log-tag-cleared`.
   - Ensure the `.log-tag-browser` class exists and uses `#3b82f6` or `var(--accent-blue)`.
   - Add a new class block:
     ```css
     .log-level-success {
         border-left-color: var(--accent-green) !important;
     }
     ```
   - Update any CSS comments to explicitly document that P1 uses `var(--accent-blue)` and P2 uses `var(--accent-red)`.
</action>
<read_first>
- src/gui/index.html
- .planning/phases/03-gui-logging-standardization/03-CONTEXT.md
</read_first>
<acceptance_criteria>
- `grep ".log-level-success" src/gui/index.html` returns matches.
- `grep ".log-tag-cleared" src/gui/index.html` returns matches.
- `grep ".log-tag-status" src/gui/index.html` returns no matches.
</acceptance_criteria>
</task>

<task>
<action>
1. In `src/gui/renderer.js`:
   - In the `log(level, msg, logData)` method, ensure that the `levelClass` logic maps `'success'` to the string `'log-level-success'` so the newly created CSS class gets applied.
   - Review how P1 and P2 tags are styled inline. Ensure P1 forces `color: var(--accent-blue)` and P2 forces `color: var(--accent-red)` when appending elements in the UI.
</action>
<read_first>
- src/gui/renderer.js
</read_first>
<acceptance_criteria>
- `grep "log-level-success" src/gui/renderer.js` or manual inspection confirms the logic correctly renders a success border.
</acceptance_criteria>
</task>

<task>
<action>
1. In `src/gui/main.js`:
   - Find all `logger.info('[Gui] ...')` statements and replace `[Gui]` with `[System]`.
   - Find the statement `Finished: Quest/Skip ${quests} | Raid ${raids}` and replace it exactly with `Done — Quests: ${quests} | Raids: ${raids}`.
   - Find all `logger.info('[Status] ...')` statements and replace `[Status]` with `[System]`.
</action>
<read_first>
- src/gui/main.js
</read_first>
<acceptance_criteria>
- `grep "\[Gui\]" src/gui/main.js` returns no matches.
- `grep "\[Status\]" src/gui/main.js` returns no matches.
- `grep "Done — Quests" src/gui/main.js` returns a match.
</acceptance_criteria>
</task>
