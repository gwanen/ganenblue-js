---
wave: 1
depends_on: []
files_modified:
  - "src/core/browser.js"
  - "src/core/login-handler.js"
  - "src/core/network-listener.js"
  - "src/core/page-controller.js"
autonomous: true
---

# Phase 4 Plan: Core Layer Logging Audit

## Goal
Enforce text styling and remove ambiguous tags from the core backend logging calls (`browser.js`, `login-handler.js`, `network-listener.js`, `page-controller.js`).

## Requirements Covered
- LOG-03
- LOG-04

<task>
<action>
1. In `src/core/browser.js`:
   - Replace all instances of `[Status]` tag with `[Browser]`.
   - Update launch messages to remove trailing periods.
   - Example: `[Status] Edge not found.` → `[Browser] Edge not found`.
</action>
<read_first>
- src/core/browser.js
- .planning/phases/04-core-layer-logging-audit/04-CONTEXT.md
</read_first>
<acceptance_criteria>
- `grep "\[Status\]" src/core/browser.js` returns no results.
- `grep "\[Browser\]" src/core/browser.js` returns results.
</acceptance_criteria>
</task>

<task>
<action>
1. In `src/core/login-handler.js`:
   - Remove nested tags like `[Status] [Login]` → `[Login]`.
   - Replace `✓ Automated login completed successfully!` → `Login complete`.
   - Standardize `Waiting for login button...` (ends with three dots for in-progress).
   - Standardize success messages for closing GBF page.
</action>
<read_first>
- src/core/login-handler.js
</read_first>
<acceptance_criteria>
- `grep "\[Status\] \[Login\]" src/core/login-handler.js` returns no results.
- `grep "✓" src/core/login-handler.js` returns no results in logging calls.
</acceptance_criteria>
</task>

<task>
<action>
1. In `src/core/network-listener.js`:
   - Replace `[Memory]` tags with `[System]`.
   - Ensure consistent wording for join error detections.
</action>
<read_first>
- src/core/network-listener.js
</read_first>
<acceptance_criteria>
- `grep "\[Memory\]" src/core/network-listener.js` returns no results.
- `grep "\[System\]" src/core/network-listener.js` returns results for listener warnings.
</acceptance_criteria>
</task>

<task>
<action>
1. In `src/core/page-controller.js`:
   - Replace `[Wait]` and `[Network]` navigation error tags with `[Core]`.
   - Replace `[Performance]` with `[Browser]`.
   - Standardize retry templates to `(N/M)`.
</action>
<read_first>
- src/core/page-controller.js
</read_first>
<acceptance_criteria>
- `grep "\[Wait\]" src/core/page-controller.js` returns no results.
- `grep "\[Performance\]" src/core/page-controller.js` returns no results.
</acceptance_criteria>
</task>
